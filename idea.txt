game:
	create_game
		create_board

	take_move(card, start, end):
		if possible_move:
			move_piece
			swap_card

			if board.is_won:
				reset()
			swap_turn


board:
	init
		extra_card
		
	def create_board
		piece_set
		board_list
		pieces in board_list


	def is_won

	def possible_move(card, start, end)
		ret true/false

	def move_piece(start, end)

piece
	init
		master?
		location


	#what happens if dead?

player:
#	hand = []
	score = 0


card


